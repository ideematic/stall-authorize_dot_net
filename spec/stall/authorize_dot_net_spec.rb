require 'spec_helper'

describe Stall::AuthorizeDotNet do
  it 'has a version number' do
    expect(Stall::AuthorizeDotNet::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
